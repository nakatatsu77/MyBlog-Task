import { Meta } from '@storybook/react'
import { AtomImage } from '../ArticleDetailsImage/Image'

const meta: Meta<typeof AtomImage> = {
  component: AtomImage,
  title: 'Atoms/Image',
}

export default meta

export const ArticleAtomImage = {}
